import { LearnSection } from "@/components/lehumo/portal/sections/LearnSection";

/** Learn — embedded micro-education. Static; auth is enforced by the
 *  member route-group layout. URL `/lehumo/portal/learn`. */
export default function LearnPage() {
  return <LearnSection />;
}
